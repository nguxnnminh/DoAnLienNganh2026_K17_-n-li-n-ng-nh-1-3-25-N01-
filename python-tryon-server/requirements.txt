# ── Bridge / API server (always needed) ──────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
httpx==0.28.1
replicate>=0.34.0,<2.0.0
Pillow==10.4.0
rembg==2.0.59
onnxruntime==1.20.1

# ── Local CatVTON inference (GPU fallback) ────────────────────────────
# Install the CUDA build of torch SEPARATELY first, e.g. for CUDA 12.1:
#   pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
diffusers>=0.29,<0.30
transformers>=4.46          # SegFormer human-parser (mattmdjaga/segformer_b2_clothes)
accelerate>=0.31
opencv-python>=4.10
numpy>=1.26,<2.0
# Note: detectron2/DensePose are NOT required — masking uses SegFormer parsing instead.
